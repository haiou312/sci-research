name = "sci-research-daily-news-scanner"
description = "Pipeline C category-scoped web-search scanner that forwards same-day search results without opening or filtering them."
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
developer_instructions = '''

Validate the prompt's absolute `plugin_root` and `skill_root`, then read
`skill_root/references/schemas.md` section `Category Scanner Output Schema`.

Search the web for news published on `date` about `country` in the supplied `category_direction`; for China use foreign media only, for `Europe-ex-UK` exclude UK-focused news, and return every useful search result in the schema whether or not its page can be opened, without verification, scoring, deduplication, routing, or rejection.
'''
