{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/hooks/email-send-guard.js\"",
            "timeout": 5
          }
        ],
        "description": "Block inline SMTP / email-library sends; force use of send-report-email.py or send-briefing-email.py"
      }
    ],
    "PostToolUse": [
      {
        "matcher": "apply_patch",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/hooks/daily-news-format-check.js\"",
            "timeout": 10
          }
        ],
        "description": "Enforce Pipeline C reference format after apply_patch operations (file is read from disk since the patch payload carries no full content)"
      }
    ]
  }
}
