interface:
  display_name: "CRD VI Transposition"
  short_description: "Weekly CRD VI status and news tracker"
  default_prompt: "Use $sci-research:crd-vi-transposition to run the latest completed weekly CRD VI update, compare it with the previous snapshot, select material regulatory news, and produce Weekly Changes plus the exact Country / Current Status / Summary table."

policy:
  allow_implicit_invocation: true
